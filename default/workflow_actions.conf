[jobid]
display_location = both
fields = jobid
label = `job($jobid$)`
search.preserve_timerange = 1
search.search_string = `job($jobid$)`
search.target = blank
type = search

[orig_host]
display_location = both
fields = orig_host
label = `hostdownwin($orig_host$,600,1)`
search.preserve_timerange = 1
search.search_string = `hostdownwin($orig_host$,600,1)`
search.target = blank
type = search
